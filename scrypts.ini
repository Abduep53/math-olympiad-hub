// scripts.js

// Примерный код для фильтрации задач
document.getElementById('olympiad').addEventListener('change', (e) => {
    const selectedOlympiad = e.target.value;
    // Фильтрация задач по выбранной олимпиаде
    // Запрос к API или базе данных, чтобы обновить список задач
    updateProblemsList(selectedOlympiad);
});

function updateProblemsList(olympiad) {
    // Здесь можно использовать fetch или другой метод для получения задач с сервера
    console.log(`Задачи для ${olympiad} будут загружены...`);
    // Получение данных из базы данных и обновление страницы
}
